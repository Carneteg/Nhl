[project]
name = "nhl-gm"
version = "1.0.0"
requires-python = ">=3.11"
description = "Data-driven Edmonton Oilers management simulation"

[project.scripts]
nhl-gm = "nhlgm.cli:main"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]

